name: Housekeeping

on:
  schedule:
    - cron: '38 04 * * 5'
  workflow_dispatch:

jobs:
  retention:
    uses: jfandy1982/shared-github-workflows/.github/workflows/reusable-retention-workflow-runs.yml@main
    permissions:
      actions: write
      contents: read
